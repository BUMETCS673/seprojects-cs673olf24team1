// /* eslint-disable no-undef */
// // Header.test.js
// import React from 'react';
// import { render, fireEvent } from '@testing-library/react';
// import Header from '../components/Header';

// test('renders and interacts with action buttons', () => {
//     const { getByText } = render(<Header />);

//     const emailButton = getByText(/Email/i);
//     fireEvent.click(emailButton);
//     expect(emailButton).toBeInTheDocument(); // Ensure button is rendered

//     const endChatButton = getByText(/End Chat/i);
//     fireEvent.click(endChatButton);
//     expect(endChatButton).toBeInTheDocument();
// });
